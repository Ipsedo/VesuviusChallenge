# VesuviusChallenge

My python template for vesuvius_challenge.

## Installation

```bash
$ cd /path/to/VesuviusChallenge
$ # install venv
$ python -m venv venv
$ source ./venv/bin/activate
$ # install requirements
$ pip install -r requirements.txt
$ # install pre-commit (needs "git init" command)
$ pre-commit install
```
